# This workflow has been intentionally disabled because deployment is handled via the
# npm "gh-pages" workflow (package.json: predeploy/deploy). If you want to re-enable
# GitHub Actions-based deploys in the future, restore the previous contents.

name: DISABLED - Deploy to GitHub Pages

on:
  # No automatic triggers. Manual only, and it does nothing.
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "GitHub Actions deploy is disabled. Use: npm run deploy"

